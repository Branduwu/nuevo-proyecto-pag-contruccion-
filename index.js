const express = require('express');
const app = express();

// Configuración y rutas de tu aplicación Express

app.listen(3000, () => {
  console.log('Servidor Express iniciado en el puerto 3000');
});